# 生产环境自定义的环境变量（命名必须以 VITE_ 开头）

# 后端接口公共路径（如果解决跨域问题采用 CORS 就需要写全路径）
VITE_BASE_API = '/api'

# 路由模式
VITE_ROUTER_HISTORY = 'hash'

## 打包路径
VITE_PUBLIC_PATH = '/monitor/'

# 预览路径
VITE_PREVIEW_PATH = '/monitor/#/preview'

# Mqtt服务器地址
VITE_MQTT_HOST = 'bohao.wang/mqtt'

VITE_MQTT_PORT = 8083
